<tr>
	<td>
		<%=f.select(:title, options_for_select(@permisos,f.object.title),{include_blank: "Seleccionar pagina"},{class: 'form-control selecciones'})%>
	</td>
	<td>
		<%=f.check_box :active, label: false%>
	</td>
	<td>
		<%= f.input_field :_destroy, as: :hidden%>
		<%= link_to '#', class: "remove_record btn btn-danger  btn-circle" do%>
			<i class="fas fa-trash"></i>

		<%end%>
	</td>
</tr>
						